{
  "name": "@vega/desktop",
  "version": "0.1.0",
  "type": "module",
  "description": "Vega 桌面版（Electron 壳：启动本地 ASAPI 服务并托管 agentscope 风格前端）",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:ui": "cd frontend && npm run build",
    "dist:mac": "npm run build:ui && electron-builder --mac dmg --universal --publish never",
    "dist:win": "npm run build:ui && electron-builder --win nsis --x64 --publish never",
    "release:mac": "npm run build:ui && electron-builder --mac dmg --universal --publish always",
    "release:win": "npm run build:ui && electron-builder --win nsis --x64 --publish always"
  },
  "devDependencies": {
    "electron": "33.4.11",
    "electron-builder": "^26.15.3"
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  },
  "build": {
    "appId": "com.cocode.desktop",
    "productName": "CoCode",
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "**/*",
      "!frontend/node_modules/**",
      "!frontend/src/**",
      "!frontend/package-lock.json"
    ],
    "extraResources": [
      {
        "from": "../core",
        "to": "core"
      },
      {
        "from": "frontend/dist",
        "to": "desktop/frontend/dist"
      }
    ],
    "mac": {
      "target": [
        "dmg"
      ],
      "icon": "assets/icon.icns",
      "identity": null
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "publish": {
      "provider": "github",
      "owner": "CoCodeAgent",
      "repo": "CoCode",
      "releaseType": "release"
    }
  }
}
